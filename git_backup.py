import os
import sys

repos = '''
bio_tda,
SFTD,
RePHINE_mod,
SFD,
SHAPR_torch,
linux-conf,
test,
gnn_tda,
TDA_SMILES2023,
FactorVAE_RTD,
dava,
ph_simple,
benchmarking-gnns,
tuple,
Graphormer,
GenCorr,
autoru-dev,
SberMOOC2022,
autoru,
disent,
RTD_AE,
S_MOOC,
BayesSeminar3,
RTD,
TDA_Sirius2022,
RTD-dev,
datashare,
DisentGAN,
MTopDiv,
CovidGANCalc,
ACGAN-PyTorch,
latent_3d_points,
TDA4GAN,
ripser-plusplus,
mlforhealthlabpub,
MF_NAS_KD_ECML_develop,
MF_KD_NAS,
disentanglement,
BarcodesDNN,
geometry-score,
AutoDL-Projects,
semi_df,
local_search,
MF_NAS_KD,
RepDistiller,
nasbench,
pt.darts,
mlss-rl-tutorial,
bb2vec,
crypto_boost,
lsml-projects,
lsml-dz,
dlr,
ya-gbm,
'''

key = input()

for repo in repos.replace('\n', '').split(','):
    os.system('cd ../github_backup; rm -rf %s; git clone https://%s@github.com/IlyaTrofimov/%s' % (repo, key, repo))
